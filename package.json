{
  "name": "bright-layers",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "run": "vite build",
    "start": "node index.js",
    "build": "npm i && npm --prefix ./BrightLayers i && npm --prefix ./BrightLayers run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "empty",
  "dependencies": {
    "express": "^4.21.2",
    "nodemailer": "^6.9.16"
  }
}
